package com.infolytics.workdays.controller;

public class CorrectionsController {
    // gets existing entry and changes it
    // if overtime in (last month) >> notify ClaudiaWerner
    // if any change in BusinessTripEntry >> check, if fellowTravellers exist and notify them



}
